<% frame.top_bar do |top_bar| %>
  <% top_bar.user_menu(name: "current_user.name", detail: "") do |user_menu| %>
    <% user_menu.avatar(
      initials: "SF",
      name: "current_user.name",
      # source: current_user.gravatar_image_url,
    ) %>

    <%= polaris_action_list do |action_list| %>
      <% action_list.item(
        icon: "LogOutMinor",
        url: "", #sign_out_path(shop: current_shop.shopify_domain),
        data: {turbo: false}
      ) { "Log out" } %>
    <% end %>
  <% end %>
<% end %>
