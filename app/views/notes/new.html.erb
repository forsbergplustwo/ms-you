
<%= polaris_page(
  narrow_width: true,
  title: it("New note"),
  subtitle: it("Add a short title and a date, with optional longer description."),
  back_url: params[:back_to].presence || notes_path,
) do |page| %>

  <%= polaris_card do %>
    <%= render "form", note: @note %>
  <% end %>
<% end %>
